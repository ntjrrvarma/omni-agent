version: '3.8'

services:
  # The Relational Brain (Live Data & Chat History)
  postgres:
    image: postgres:15-alpine
    container_name: omni_postgres
    env_file:
      - .env
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: omnidb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./data/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d omnidb"]
      interval: 5s
      timeout: 5s
      retries: 5

  # The Vector Brain (PDF Embeddings)
  qdrant:
    image: qdrant/qdrant:latest
    container_name: omni_qdrant
    ports:
      - "6333:6333" # REST API
      - "6334:6334" # gRPC
    volumes:
      - qdrant_data:/qdrant/storage

  # The AI Engine (LangChain Agent)
  backend:
    build: ./backend
    container_name: omni_backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://admin:${POSTGRES_PASSWORD}@postgres:5432/omnidb
      QDRANT_URL: http://qdrant:6333
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
    volumes:
      - ./data:/app/data

  # The User Interface (Streamlit)
  frontend:
    build: ./frontend
    container_name: omni_frontend
    ports:
      - "8501:8501"
    environment:
      BACKEND_URL: http://backend:8000
    depends_on:
      - backend

volumes:
  postgres_data:
  qdrant_data: